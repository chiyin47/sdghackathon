name: gh-pages build
 
# Controls when the action will run.
on:
    # Triggers the workflow on push or pull request events but only for the master branch
    push:
        branches: [main]
 
    # Allows you to run this workflow manually from the Actions tab
    workflow_dispatch:
 
jobs:
    build:
        permissions:
            pages: write
            id-token: write
        environment:
            name: github-pages
            url: ${{ steps.deployment.outputs.page_url }}
        runs-on: ubuntu-latest
        steps:
            - name: Checkout <img draggable="false" role="img" class="emoji" alt="⏬" src="https://s0.wp.com/wp-content/mu-plugins/wpcom-smileys/twemoji/2/svg/23ec.svg">
              uses: actions/checkout@v2
 
            - name: Install <img draggable="false" role="img" class="emoji" alt="🔧" src="https://s0.wp.com/wp-content/mu-plugins/wpcom-smileys/twemoji/2/svg/1f527.svg">
              run: cd frontend && npm install
 
            - name: Build <img draggable="false" role="img" class="emoji" alt="🔥" src="https://s0.wp.com/wp-content/mu-plugins/wpcom-smileys/twemoji/2/svg/1f525.svg">
              run: cd frontend && CI='' npm run build
 
            - name: Setup Pages <img draggable="false" role="img" class="emoji" alt="⚙" src="https://s0.wp.com/wp-content/mu-plugins/wpcom-smileys/twemoji/2/svg/2699.svg">
              uses: actions/configure-pages@v3
 
            - name: Upload artifact <img draggable="false" role="img" class="emoji" alt="🔼" src="https://s0.wp.com/wp-content/mu-plugins/wpcom-smileys/twemoji/2/svg/1f53c.svg">
              uses: actions/upload-pages-artifact@v3
              with:
                  path: "./frontend/dist"
 
            - name: Deploy to GitHub Pages <img draggable="false" role="img" class="emoji" alt="🚀" src="https://s0.wp.com/wp-content/mu-plugins/wpcom-smileys/twemoji/2/svg/1f680.svg">
              id: deployment
              uses: actions/deploy-pages@v4